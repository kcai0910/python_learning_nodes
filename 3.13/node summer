终止循环:
continue:跳过本次循环剩下的语句，直接进行下一次循环
break:立即终止循环，不可再执行后续的代码

数据容器: list 列表     tuple 元组     str 字符串    set 集合     dict 字典
数据容器可存放多个数据，每一个数据称为元素
数据容器中的元素可以是任意类型
数据容器可以为我们提供多个操作元素的方法，例如:crud


列表: 就是用来存放一组有序的数据，且可以对其中的数据进行crud

#定义列表
list1 = []

#下标被称为索引index，就是元素在列表中的位置编号,分为'正索引'和'负索引'（正索引从左往右从0开始，负索引从右往左从-1开始）
#通过下标取值时，下标不要超过列表长度不然会报错

列表方法
crud:
增 ：
list.append(元素) 从列表末尾添加
list.insert(下标，元素) 列表指定位置添加元素
list.extend(可迭代对象)列表末尾添加可迭代对象

删除
list.pop(index) 删除指定位置的元素，返回删除的元素
list.remove(元素)删除列表第一次出现的指定值
list.clear()清空列表
des list(index) 删除指定位置的值

修改
list[index] = 新值

查看
print(list[index])

其他方法
list.index(元素) 查找元素第一次出现的下标
list.count(元素)查找元素在列表中出现的次数
list.reverse() 反转列表,不需要参数，直接操作原列表
list.sort(reverse=bool) 对列表进行排序，默认从小到大，会改变原列表
内置方法：
max(list) ：返回列表的最大值
min(list) ：返回列表的最小值
len(list) ：返回列表的长度
sum(list) ：求和列表

列表的循环:
for i in list:
       print(i)

while index < len(list);
      print(list[index])
      index += 1 

for i in rang(len(list)):
      print(list(i))

列表的特点:
       1,列表可以存放不同类型的元素
      2，列表是有序存储的（正索引，负索引）
      3，列表中的元素可以重复
      4，元素是可以被修改的（crud）
      5,长度不固定可以随意操作

定义元组
  tuple = ()
  元组中只有一个数据加逗号
  tuple = ('dsf',)
  元组的查看
  print(tuple(idnex))
  元组不可以被修改，如果元组中存放了可变l类型(列表)，那么可变类型是可以被修改的
  查看元素第一次出现的下标 tuple.index(元素)
  统计元素在tuple中出现的次数 tuple.count(元素)
  最大 max(tuple)
  最小 min(tuple)
  总和 sum(tuple)
  长度 len(tuple)
  排序 sort(tuple,reverse = bool)

元组特点 :
  1, 元组可以存放不同类型的元素
  2，元组中的元素不可以被修改（只能查不能增删改）
  3，元组是有序存储的（正索引，负索引）
  4，元组中的元素可以重复
  6，长度固定（一旦创建，元素不可以增减）
元组是一种只读数据容器，如果想保持数据不变首选元组

区别 元组： 数据不可变    列表 ： 数据可变
