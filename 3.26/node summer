1，函数也是一个对象
2，函数和对象一样可以添加属性
3，函数可以赋值给变量
4，可变参数（在函数内更改值的时候在原位置id处更改），不可变参数（在函数内更改值的时候在新的地址创建一个新的值）
5，函数可以作为参数来使用

#高阶函数
当一个函数参数是函数，或者返回值为函数，那么该函数就是高阶函数
好处：
1：代码复用性高，可以把行为独立出去，传入不同的函数，实现不同的功能
2：函数更灵活，更通用
3：高阶函数是装饰器，闭包是基础

#条件表达式
1，执行最后能得到一个值的代码就是表达式
2，条件表达式：根据不同的条件得到不同的值，也被称为三元表达式，又称为三目运算
使用场景：二选一的情况下
值1（条件成立） if 判断 else 值2（条件不成立）

#匿名函数 就是没有名字的函数，不需要def关键字去定义
语法lambda来定义匿名函数，格式 lambda 参数:表达式
当一个函数只是使用一次或者只做一个小事，推荐使用匿名函数

使用规则：
lambda 只能写一行
不可以写代码块
冒号右边必须是表达式而且只能有一个表达式
执行结果自动作为返回值

数据处理函数
map():对一组数据中的每一个元素统一执行某一个操作，并生成一组新的数据
map(操作函数，可迭代对象)
map函数返回值是一组迭代器对象，需要自己动手遍历，或者类型转换，数据会被消耗（只能使用一次）

注意事项：
延迟执行，map不会立刻计算，只有在需要结果的地方才执行
返回值是迭代器对象，一旦使用则会被耗尽
map函数不会影响元素数量


filter():从一组数据中筛选符合条件的元素（过滤），并返回一组新的数据
filter(过滤函数，可迭代对象)
filter会自动筛选函数返回值为True的元素*

不传入过滤函数，自动过滤转布尔值为False的数据：filter(None,sum)
注意事项:
延迟执行，filter函数不会立即判断，只有需要结果的地方才执行
返回值是迭代器对象，一旦使用则会被耗尽
filter函数会影响元素数量

sorted():对一组数据进行排序，返回一组新的数据
语法为sorted(可迭代对象，key=可设置排序的依据，reverse设置排序的方向)
max()和min()也可以通过设置key传递参数用于筛选依据
max(names,key = lambda x: x['name'])
min(names,key = lambda x:x['name'])
