
数据处理函数
map()  对一组数据每一个值进行某一种操作并返回一组新的数据
filter() 在一组数据中筛选符合条件的数据，并返回一组新的数据
reduce() 将一组数据不断合并，最终合并成一个结果
sorted() 对一组数据进行排序并返回一组新的数据

reduce() 合并一组数据，最终合并成一个结果
from functools import reduce
reduce(合并函数,可迭代对象，初始值)

列表推导式:
用一条简洁的语句，从可迭代对象里，生成新列表的语法结构
语法结构[表达式 for i in 可迭代对象]
num = [1,2,3,4,5]
res = [i*2 for i in num]
待条件的推导式
res = [i*2 for i in num if i>3]

字典推导式
name = ['你'，'我','他']
num = [1,2,3]
res = {name[i]:num[i] for i in range(len(name))}

集合推导式
num = [1,2,3,4,5]
res = ( i for i in num )

元组没有推导式元组用这个会返回一个产生器generator 用tuple(generator)会返回元组

深拷贝和浅拷贝
浅拷贝
会创建一个外部容器，但是内部元素仍然使用的原来的对象
嵌套数据仍是共享的，修改嵌套数据仍会相互影响
import copy
num = [1,2,3,4,5]
num2 = copy.copy(num)
深拷贝
创建一个新的外部容器，并对其内部所有对象进行复制

深拷贝可以彻底消除数据之间的相互影响

作用域有四种 LEGB
local 局部作用域
enclosing 外部函数作用域
global 全局作用域
build-in 内建作用域

查找顺序为LEGB

修改全局变量使用global 变量
修改enclosing外部函数变量 nonlocal 变量

访问外部变量不需要使用nonlocal,修改外部函数变量需要使用nonlocal

闭包内层函数+被内层函数使用的外部变量
1函数必须嵌套
2函数必须使用外部函数enclosing变量
3enclosing外层函数必须返回内层函数
def enclosing():
     i =10
    def local（）：
        nonlocal i
        i+=1
        print(i)
    return local
res = enclosing()
每次获取闭包之间互不影响（闭包之间是相互独立的）
外层变量为可变对象时也互不影响
在外部函数结束后闭包调用的外部变量会存在闭包内的单元cell中
查看方法 res.__closure__  
查看具体元素:res.closure__[0].cell_contents

可以记住状态，不需要全局变量，实现变量的私有化
可以实现数据隐蔽
是装饰器的基础

理解成本高对新手不友好
如果闭包内的数据得不到释放会增加内存占用

类型注释 如果类型不对应会标黄 并不是在定义变量，他只是在说明
res: int = 10
列表
list: list[int|str] =[1,2,3,4,5]
字典
dict: dict[int|str,int]={1:2,3:4,5:6}

元组比较特殊
是对元组类型进行注解而且是对一个值进行注解
tuple:tuple[int,int] = (1,2)
tuple:tuple[int,...]=(1,2,3,4,5)
tuple:tuple[int|str,...]=(1,2,'sr')

python里提供类型推导，根据变量初始赋值的实际数据，自动推断变量的类型
x= 100
x='str'会爆黄

函数的类型推导
def ad(a:int,b:int)->int(判断返回值类型默认为元组):
        return  a+b
res = ad(10,20)
