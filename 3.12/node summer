数据类型准换：
原因：1用户输入的数据都是字符串，在进行运算过程中必须进行数据类型的转换
      2使用数据库数据时也会进行数据类型转换
类型
1，int(x) 中间带有空格，带有中文的值，浮点数类型的字符串不可转，两边有或没有空格的数字，字符串中的纯数字，整数类型都可以转
2.float(x) 中间有空格，带有汉字的值不可转，字符串中的纯数字，整数，浮点数都可以转
3.str(x)  可以将任意的数据类型转换成字符串类型

运算符
+ - * / 
// 取整  % 取余 **指数运算
赋值运算符
+= -= *= /= //= %= **=

比较运算符
==  判断相等
!=  判断不等
（整数型和字符串无法进行比较）
字符串进行比较是对比unicode码
ord（） 把字符转化成unicode码 chr() 把unicode码转换成字符 括号里只能写一个字符

布尔值
True False 首字母要大写
是int的子类型
可以进行加减运算
布尔值转换：bool()(非0和空的数字转布尔值都为True.非空的字符串转布尔值都为True)

逻辑运算符
and or not 与或非
逻辑and与or都具有逻辑短路功能->只要有一侧结果出来后后续的代码将不再实行，并且输出最后执行的值
逻辑非not是对一个值进行取反

进制
二进制，十进制，十六进制，八进制

十进制转二进制 bin()
十进制转八进制 oct()
十进制转十六进制 hex()

二进制转十进制int('0b1231123',2)
八进制转十进制int('0o13123',8)
十六进制转十进制int('0xs123f',16)

输入语句
input('******:') 用户输入的信息都是字符串，并且在输入过程中会暂停代码的执行
转int型输出 m=int(input('*****:'))

分支 顺序结构-分支结构-循环结构
python 代码是通过代码缩进/空格来识别代码范围的
if 判断条件:
      条件满足时执行的代码
elif 判断条件:
      条件满足时执行的代码
else:
     条件不满足时执行的代码

循环 
1，while循环
while 比较灵活但需要手动确定范围容易发生死循环 比如输密码不对会让你一直输入
while 循环条件：
      条件满足时执行的代码
2，for循环
#for 比较单一 但不容易发生死循环，比如输入密码不会让你一直输入
for x in  可迭代对象（可容纳一组数据的容器（起始参数，终止参数））：
    需要执行的操作
#数字最后一位不能打印出来比如range(5) 只能打印1-4打印不出5
#字符串每一位都能打印

