"""
\'输出单引号(同" '' "）
\n 换行
\\note 输出一个\
\b 删除前一个字符
\r 后面的内容覆盖前面内容，进度条 累加计时器 发送到后台，后台发送回请求（75%\r86%）
\t 制表符 不同环境运行下长度不固定 .expandtabs(4)限制制表符范围
"""

转义字符
"""
什么是数据类型的转换:解释把一种数据类型转换成另外一种数据类型
float() 转浮点型带汉字的带字母的带单词的都不能转
中间带有空格的数字，带有汉字的数字，不可转
纯数字，整形，浮点型都可以转
int() 字符串转整形直接舍弃浮点型后面的小数位，
带有中文的值，字符串中的浮点数，不可以转换
两边带有空格数字，字符串中的纯数字，整数类型都可以转
str() 任意一个类型的数据
"""

运算符
#+ - * / // % **

赋值运算符
+= -= *= /= //= %= **=

比较运算符
a>b 判断a是否大于b
a<b 判断a是否小于b
a!=b 判断a是否不等于b
a==b 判断a是否等于b
res = a == b 先判断再赋值
a >= b 判断 a是否大于等于b
a <= b 判断a是否小于等于b

"""
比较运算符返回的布尔值只有两个值True与False
"""
字符串之间的对比 判断逻辑:比较两个字符串对应字符的unicode码
#字符串比较之前先转成unicode码，然后再做对比
#使用内置函数ord(),将字符转成unicode码
#使用内置函数chr(),将unicode码转成字符

ord():将字符转为unicode码
chr()：将unicode 码变成字符

#布尔值:True False
# 条件是否成立，事件是否发生，操作是否成功，逻辑相关功能
布尔类型是int类型的子类型
可以参与运算True+True True+False 1+True 1+False
可以判断 1>=True False<=0

转布尔值
bool() 非0的数字转为布尔值都为True 非空的字符串转布尔值都为True

逻辑运算符
and 逻辑与 判断两侧是否都为True 一假则假
or  逻辑或 判断两侧是否至少有一侧为真，一真则真
and 和 or 都具有逻辑短路功能
not 逻辑非 对一个值进行取反，而且not的返回值一定为布尔值

如果判断条件为运算表达式，则返回计算的结果



